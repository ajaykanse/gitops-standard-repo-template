default:
  app:
    enabled: true
    enableAutoSync: true
    autoSyncPrune: false
    project: cluster-config
    labels:
      app-source: group-dev
      repo: cluster-config
    destination:
      namespace: openshift-gitops
      server: https://api.cluster-tgknv.tgknv.sandbox2894.opentlc.com:6443
    source:
      repoURL: https://github.com/ajaykanse/gitops-standard-repo-template.git
      targetRevision: acm-test
    syncOptions:
    - ApplyOutOfSyncOnly=true
    - PrunePropagationPolicy=foreground
    - PruneLast=true
    - ServerSideApply=true  
    - FailOnSharedResource=true
    - RespectIgnoreDifferences=true
    - CreateNamespace=false      

applications:

  # vault-config-operator:
  #   annotations:
  #     argocd.argoproj.io/compare-options: IgnoreExtraneous
  #     argocd.argoproj.io/sync-wave: '5'
  #   destination:
  #     namespace: cert-manager
  #   source:
  #     path: groups/prod/overlays/vault-config-operator
  
  gitlab-runner-operator:
    annotations:
      argocd.argoproj.io/compare-options: IgnoreExtraneous
      argocd.argoproj.io/sync-wave: '5'
    destination:
      namespace: openshift-operators
    source:
      path: groups/production/overlays/gitlab-runner-operator